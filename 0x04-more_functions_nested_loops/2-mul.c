#include "main.h"
/**
*mul - multiply 2 int values
*@a: first int to be multiplied
*@b: second int to be multiplied
*Return: The result of the multiplication of the two int values
*/

int mul(int a, int b)
{
return (a * b);
}
